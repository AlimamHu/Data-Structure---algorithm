def encode(strs):
    res=''
    for s in strs:
        res+=str(len(s))+'#'+s
    return res

strs=["lint","code","love","you"]
strs=encode(strs)

def decoding(strs):
    res,i=[],0
    while i <len(strs):
        j=i
        
    pass

decoding(strs)